<% @products.each do |product| %>
<hr />
  <%= product.name %>
  <hr />
  <%= product.description %>
  <hr />
  <%= image_tag product.image, width:"20%" %>


<% end %>

<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>
